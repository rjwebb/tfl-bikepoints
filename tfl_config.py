"""
The TfL API rate-limits the usage of unregistered users.
For this reason, you should register for an API ID and key at https://api-portal.tfl.gov.uk/signup .
Once you have done so, edit the below definitions of APP_ID and APP_KEY to use the API as a registered user.
"""

APP_ID = ""
APP_KEY = ""

auth = (APP_ID, APP_KEY)
